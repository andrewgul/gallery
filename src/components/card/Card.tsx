import styles from './Card.module.css'

interface Props {
    
}

const Card = (props: Props) => {
    return (
        <div className={styles['card']}>
            
        </div>
    )
}

export default Card
